{
  "name": "tiendamayor-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend & npm start --prefix frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "typewriter-effect": "^2.22.0"
  }
}
